<nav>
  <div class="container">
    <a class="logo" href="/">
      <div class="logo-wrapper">
        <img src="/images/Krookie-Red.svg">
        <p>Admin</p>
      </div>
    </a>
    <ul>
      <div class="navlink"><li><%= link_to 'Home', welcome_index_path %></li></div>
      <div class="navlink"><li><%= link_to 'Shelves', shelves_path %></li></div>
      <div class="navlink"><li><%= link_to 'Books', books_path %></li></div>
    </ul>
  </div>
</nav>
<div class="container">
  <p>
    <strong>Main beacon:</strong>
    <%= @shelf.mainbeacon %>
  </p>
  <p>
    <strong>Beacon 1</strong>
    <%= @shelf.beacon1 %>
  </p>
  <p>
    <strong>Beacon 2</strong>
    <%= @shelf.beacon1 %>
  </p>
  <p>
    <strong>Beacon 3</strong>
    <%= @shelf.beacon1 %>
  </p>
  <h2>Books</h2>
  <table>
    <thead>
      <tr>
        <th>ISBN</th>
        <th>Title</th>
        <th colspan="3" >Bewerkingen</th>
      </tr>
    </thead>
    <tbody>
      <% @shelf.books.each do |book| %>
          <tr>
            <td><%= book.isbn %></td>
            <td><%= book.title %></td>
            <td><div class="btn"><%= link_to 'Show', book_path(book) %></div></td>
            <td><div class="btn"><%= link_to 'Destroy', book_path(book),method: :delete, data: { confirm: 'Are you sure?' } %></div></td>
          </tr>
      <% end %>
    </tbody>
  </table>
    <div class="btn"><%= link_to 'Edit', edit_shelf_path(@shelf) %> </div>
    <div class="btn"><%= link_to 'Back', shelves_path %></div>
</div>
